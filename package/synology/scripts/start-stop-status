#!/bin/sh


case "$1" in
	start)
		"$SYNOPKG_PKGDEST/bin/filebot-node-start" > "$SYNOPKG_PKGDEST/filebot-node.log" 2>&1 &
		exit $?
	;;

	stop)
		kill "$(ps -u "root" -f | grep "filebot-node" | awk '{ print $2 }')"
		exit $?
	;;

	status)
		ps -u "root" -f | grep "filebot-node"
		exit $?
	;;
esac
